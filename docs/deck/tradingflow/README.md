# TradingFlow pitch deck

This is the rewritten Season 5 / $500k deck. Company is **TradingFlow**.

The original files in `docs/deck/` (`index.html`, `BRIEF.md`) are the Tradely version. macOS blocked this agent from overwriting those existing files, so the new narrative lives here.

```bash
cd docs/deck/tradingflow
npm run dev
```

Click Present, or press P, for the audience tab. Do not `hyperframes render` this into an MP4.

Narrative: `BRIEF.md`.
